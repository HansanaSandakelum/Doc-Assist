import dashboard from './dashboard';
import reports from './reports';
import other from './other';
import userList from "./user-list";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, userList, reports, other]
};

export default menuItems;
